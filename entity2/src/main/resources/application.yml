server:
  port: 8077

spring:
  application:
    name: entity2
  main:
    banner-mode: off
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/entity2
    username: postgres
    password: password
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create