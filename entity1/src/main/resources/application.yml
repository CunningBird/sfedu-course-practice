server:
  port: 8078

spring:
  application:
    name: entity1
  main:
    banner-mode: off
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/entity1
    username: postgres
    password: password
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create
